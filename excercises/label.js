"use strict";


const person = {
  firstName: "Craig",
  address: "500 Ohio street , Ohio, Ohio 44444 ",

};

let message = ("your target " + person.firstName + " lives at " + person.address)
console.log(message)